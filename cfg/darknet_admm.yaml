admm:
  True
masked-retrain:
  False
admm-epochs:
# select weight positions based on ADMM every 6 epoches
  6
rho:
#rho:
  0.0001
rho-num:
#rho: order of magnitude （from 0.0001 x 1e-1 to 0.0001 x 1e-4)
  4
sparsity-type:
# Prune Scheme
  block-punched
config-file:
  config_csdarknet53pan_v2
combine-progressive:
#  Progressive Pruning. 4x pruning load 2x pruned weight file
  False
verbose:
#  Log
  True
